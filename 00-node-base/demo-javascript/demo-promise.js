// Promise : 약속을 지키는 객체

// 매개변수로 콜백함수를 받는다.
// executor : resolve, reject 두 개의 콜백함수를 매개변수로 가지는 콜백함수
// 할 일을 성공적으로 마치면 resolve 호출
// 실패하면 reject 호출
let promise = new Promise((resolve, reject) => {
    
});